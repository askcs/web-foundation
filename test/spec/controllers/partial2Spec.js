define(
  ['angularMocks', 'controllers/controllers', 'controllers/partial2'],
  function()
  {
    describe('Partial2 Controller',
      function ()
      {
        beforeEach(module('controllers'));

        it('should ....',
          inject(function ()
          {
            //spec body
          })
        );
      }
    );
  }
);