define(
  ['controllers/controllers'],
  function (controllers)
  {
    'use strict';

    controllers.controller ('partial2',
      [
        '$scope',
        function ($scope)
        {
        }
      ]
    );
  }
);